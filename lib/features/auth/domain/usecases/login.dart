import 'package:api_toko/core/usecase/usecase.dart';
import 'package:dartz/dartz.dart';

class Login extends UseCase<Either, String> {
  @override
  Future<Either> call({String? param}) {
    // TODO: implement call
    throw UnimplementedError();
  }
}
